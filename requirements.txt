supabase-py==1.0.3
requests>=2.31.0
python-dotenv>=1.0.0
psycopg2-binary>=2.9.5     # For PostgreSQL interactions
openai>=1.14.3             # If using GPT-based agent tools
numpy>=1.24.0              # For any embeddings or math ops
tqdm                       # For progress bars in scripts (nice-to-have)
flask                     # If you're building a web server locally
fastapi                   # If you're doing agent APIs
uvicorn                   # FastAPI ASGI server
pandas                    # If you're working with CSVs / tabular data
